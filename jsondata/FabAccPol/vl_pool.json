{
    "fvnsVlanInstP": {
        "attributes": {
            "allocMode": "{{mode}}",
            "dn": "uni/infra/vlanns-[{{name}}]-{{mode}}",
            "name": "{{name}}",
            "status": "{{action}}"
        },
        "children": [
            {
                "fvnsEncapBlk": {
                    "attributes": {
                        "allocMode": "{{range_mode}}",
                        "from": "vlan-{{start}}",
                        "to": "vlan-{{end}}",
                        "status": "{{action}}"
                    }
                }
            }
        ]
    }
}
